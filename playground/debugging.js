let person = {
	name: 'Gonzalo',
};

debugger; //make the debugger jump to this line (when using c), it will stop here like a red flag
person.age = 28;
person.name = 'Ramiro';
console.log(person);

/*Debug NavegationCommands:
-inspect (starts the debugging tool)
-list (you can list the code (x) lines)
-n (runs the next line and next and so on)
-c (continues until your program completes)
-repl (means read-eval-print-loop)*/

